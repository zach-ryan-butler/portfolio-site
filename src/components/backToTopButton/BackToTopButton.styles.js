import { makeStyles } from '@material-ui/core/styles';

const useStyles = makeStyles({
  icon: {
    border: '1px solid',
    borderColor: 'inherit',
    color: 'inherit',
    height: '80px',
    width: '80px',
    borderRadius: '40px',
  },
});

export default useStyles;
